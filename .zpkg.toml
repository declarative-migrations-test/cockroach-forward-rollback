[package]
org = "declarative-migrations-test"
name = "cockroach-forward-rollback"
version = "0.1.0"
description = "Independent database acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/declarative-migrations-test/cockroach-forward-rollback"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
